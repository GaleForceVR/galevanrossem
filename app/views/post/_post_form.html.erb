<%= form_for @post do |f| %>
  <%= f.text_field :title, placeholder: "Title" %>
  <div class="field">
  <%= f.text_area :content, class: "tinymce", :rows => 40, :cols => 120 %>
  <%= f.submit %>
  </div>
<% end %>